.container
    .row#banner
        .starter-template
            h1 Create Awesome { Fake : "Data" }
            p.lead Because developers need pampering. #[span(style="font-family: 'Nothing You Could Do', cursive;") Always!]
    #workspace
        #fakes.row
            .col-md-4(ng-repeat="fake in explore.Fakes")
                .panel.panel-default
                    .panel-body.text-center
                        p.lead {{fake.name}}
                        pre.text-left.well {{fake.data}}
                        a(class="btn btn-success btn-outline" ng-href="/explore/{{fake.name}}") View Fake
        .row.text-center
            button(type="button" class="btn btn-default btn-outline") Load More